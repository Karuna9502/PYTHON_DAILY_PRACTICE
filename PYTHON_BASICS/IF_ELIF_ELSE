# Q1.WAP to display the last digit of a number. (Don’t use indexing for this) 

number = int(input("Enter a number : "))
if number <= 10:
    print(number)
else:
    last_digit = number % 10
    print(f"The last digit of entered number {number} will be",last_digit)

# Q2 WAP to check if a single character is a vowel or not.

character = input("Enter a single character : - ")
if character.lower() in "aeiou" or character.upper() in "AEIOU":
    print(f"The Charater '{character}' is Vowel.")
else:
    print(f"The character '{character}' is not vowel.")

# Q3 . WAP to check if a number is even or odd where the number is taken as input.

a = int(input("enter a number : - "))
if a % 2 != 0:
    print("The input number is odd")
else:
    print("The given number is even")

# Q4. WAP to check if the 3rd last character of a string is a vowel or not.

string = input("Enter string to check : - ")
if string[-3].lower() in "aeiou" or string[-3].upper() in "AEIOU":
    print("The 3rd of a given string is vowel")
else:
    print("The 3rd of a given string is not vowel")

# Q5. Check if the first and last number of a list is same or not. Take a pre-defined list in the code itself.

list = [1,2,3,4,5,6,7,8,1]
if list[0] == list[-1]:
    print(" The first and last number of a list is same.")
else:
    print(" THe first and last number of a list is not same.")

# Q6  Calculate income tax for the input income by adhering to the Indian rules.

income = int(input("Enter your income :- "))
if income <= 400000:
    print("No income tax")
elif income <= 800000:
    tax = (5 * income)/100
    print("your income tax is ", tax)
elif income <= 1200000:
     tax = (10 * income)/100
     print("your income tax is ", tax)
elif income <= 1600000:
    tax = (15 * income)/100
    print("your income tax is ", tax)
elif income <= 2000000:
    tax = (20 * income)/100
    print("your income tax is ", tax)
elif income <= 2400000:
    tax = (25 * income)/100
    print("your income tax is ", tax)
else:
    tax = (30 * income)/100
    print("your income tax is ", tax)

# Q7. Write a Python program to check whether a string starts with vowel or not.

string = input("Enter a string :- ")
if string[0].lower() in "aeiou" or string[0].upper() in "AEIOU":
    print("The string starts with vowel.")
else:
    print("The string does not start with vowel.")

# Q8 WAP to calculate percentage of a student through 5 subjects. Take marks as input from the user.
# Using percentage print which grade the student has scored.

a = int(input("Enter the marks of Math : - "))
b = int(input("Enter the marks of English : - "))
c = int(input("Enter the marks of Hindi : - "))
d = int(input("Enter the marks of Scienec : - "))
e = int(input("Enter the marks of Social-Science : - "))

sub_total_marks = a+b+c+d+e
total_marks = int(input("Enter the total marks : - "))

percentage = (sub_total_marks/total_marks) / 100

if percentage > 90:
    print("Your grand is A+")
elif percentage <= 90 and percentage > 80:
    print("Your grade is A")
elif percentage <= 80 and percentage > 70:
    print("Your grade is B+")
elif percentage <= 70 and  percentage > 60:
    print("Your grade is B")
elif percentage <= 60 and percentage > 50:
    print("Your grade is C+")
elif percentage <= 50 and percentage > 40:
    print("Your grade is C")
elif percentage <= 40 and percentage > 30:
    print("Your grade is D")
else:
    print("STUDY HARD, FAIL!!")

# Q10. Write a program to check if a number is positive, negative, or zero.

number = int(input("Enter a number : - "))
if number > 0:
    print(f"The entred number {number} is positive.")
elif number < 0:
    print(f"The entred number {number} is negative.")
else:
    print(f"The entred number{number} is zero.")

# Q17. Take a temperature input and print:

temperature = int(input("Entre the tempearture : - "))

temperature = int(input("Enter the temperature: "))

if temperature > 50:
    print("Extremely Hot")

elif 31 <= temperature <= 50:
    print("Hot")

elif 21 <= temperature <= 30:
    print("Warm")

elif 0 <= temperature <= 20:
    print("Cold")

else:
    print("Invalid Temperature")




